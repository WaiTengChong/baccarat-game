const useStyles = () => {
  const styles = {
    salesCard: 'sales-card',
    salesBar: 'sales-bar',
    dataText: 'dataText',
    consecutiveWinsTable: 'consecutive-wins-table'
    
  };
  
  return { styles };
};

export default useStyles;